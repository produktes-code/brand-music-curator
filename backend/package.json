{
  "name": "brand-music-curator-backend",
  "version": "1.0.9",
  "private": true,
  "description": "Local backend server for Brand Music Curator",
  "main": "server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "test": "node test_bridge_queue.js && node test_cache_cleanup.js && node test_server_port_fallback.js && node test_spotify_error.js && node test_upload_cleanup.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "multer": "^2.0.2"
  }
}
